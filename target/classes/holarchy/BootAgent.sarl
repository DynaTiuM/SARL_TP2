/** 
 * 
 */
package holarchy

import io.sarl.api.core.Initialize
import io.sarl.api.core.Lifecycle
import io.sarl.api.core.Logging
import ui.SearchManagerGUI

/** 
 * 
 */
agent BootAgent {
	uses Logging, Lifecycle

	on Initialize {
		var ui = new SearchManagerGUI()
		spawn(SearchManager)
		loggingName = "Boot"
		
		killMe
	}
}
