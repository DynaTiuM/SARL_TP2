/** 
 * 
 */
package io.sarl.demos.basic.holarchy

import io.sarl.api.core.Initialize
import io.sarl.api.core.Lifecycle
import io.sarl.api.core.Logging

/** 
 * 
 */
agent BootAgent {
	uses Logging, Lifecycle, Logging

	on Initialize {
		spawn(SearchManager)
		loggingName = "Boot"
		
		killMe
	}
}
